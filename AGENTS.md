# AGENTS.md — Unreal Engine 5.8.2 작업 규칙

이 파일은 저장소 루트 이하의 Codex 작업에 적용되는 지속 지침이다.

## 프로젝트 고정값

- UE_VERSION: `5.8.2`
- PROJECT_NAME: `ProjectProjectAI01`
- PROJECT_DESCRIPTION: `1인칭 마네킹 AI 피하는 공포게임`
- TARGET_PLATFORMS: Win64
- LANGUAGE_SCOPE: C++
- SAFETY_LEVEL: High
- REQUIRED_FILENAMES: 지정 없음

`PROJECT_NAME`이 아직 위의 미입력 상태라면 프로젝트명/모듈명/클래스명/파일경로를 추측하지 말고, 코드 생성·수정 전에 실제 프로젝트명을 요청한다. 예시 이름을 실제 코드에 사용하지 않는다.

## 절대 규칙

1. UE 5.8.2에서 확인된 API·매크로·UBT/Build 규칙만 사용한다.
2. 다른 UE 버전의 기능을 5.8.2에 적용한다고 추측하지 않는다.
3. 핵심 API/빌드 규칙은 구현 전에 근거를 확인한다.
4. 근거가 부족하거나 5.8.2 동작을 확인할 수 없으면 먼저 `근거가 부족합니다` 또는 `모릅니다`라고 쓰고 추측 구현을 하지 않는다.
5. 기존 저장소의 실제 파일·모듈·클래스·네이밍을 먼저 조사하고 최소 변경을 우선한다.
6. 사용자가 지정하지 않은 새 파일명은 임의 생성하지 않는다. 필요하면 `필요 파일명 제안`에서 이름만 제안하고 승인 전에는 생성하지 않는다.

## 허용 근거

최종 구현의 근거는 아래 순서로 제한한다.

1. Epic Games 공식 Unreal Engine 5.8.2 엔진 소스
   - 설치된 5.8.2의 `Engine/Source/...`
   - EpicGames/UnrealEngine의 5.8.2 대응 공식 소스/커밋/태그/브랜치
   - Epic 공식 샘플 소스
2. 공개 열람 가능한 기술 블로그
   - Google/Naver 검색으로 접근 가능
   - 작성자·게시일·대상 UE 버전 확인 가능한 글 우선

Stack Overflow와 Unreal Engine 커뮤니티 글은 탐색/보조 확인에는 사용할 수 있지만, 이 파일의 더 엄격한 출처 제한 때문에 단독 핵심 근거로 사용하지 않는다.

금지 근거: 모델 기억만의 설명, 다른 UE 버전 문서를 5.8.2 근거로 대체한 내용, 유료/비공개 자료, 출처/버전을 확인할 수 없는 코드 조각.

## 5.8.2 검증 항목

관련 심볼/API마다 다음을 확인한다.

- 5.8.2 소스에 실제 존재하는가
- 선언 헤더와 필요한 모듈은 무엇인가
- Runtime/Editor 중 어디에서 사용 가능한가
- `WITH_EDITOR`, `WITH_EDITORONLY_DATA` 또는 모듈 분리가 필요한가
- 5.8.2에서 deprecated/교체 대상인가

버전 가드는 5.8.2 소스에서 실제 제공되는 버전 매크로를 확인한 뒤에만 사용한다. 확인하지 않은 patch-level 매크로를 만들지 않는다.

## 이름·경로

- 프로젝트명, 모듈명, 클래스명, 네임스페이스, 로그 카테고리, 경로는 실제 `PROJECT_NAME`, 기존 저장소 이름, 실제 `REQUIRED_FILENAMES`를 그대로 따른다.
- 임의 축약·치환 금지.
- 기존 코드에 이름이 이미 있으면 그것을 우선한다.
- 새 파일이 필수인데 이름이 지정되지 않았다면 생성하지 말고 먼저 제안한다.

## High 안전성

- UObject/Actor/Component/Subsystem 접근 전 수명과 유효성을 검증한다.
- UObject 참조 보관 시 GC 추적 여부를 확인하고 적절한 `UPROPERTY`/UE 포인터 타입 사용을 5.8.2 근거로 판단한다.
- 비소유 참조는 필요 시 weak reference, 지연 로딩은 필요 시 soft reference를 검토하되 정확한 타입/API를 소스로 확인한다.
- `UFUNCTION`, `UPROPERTY` 지정자는 실제 Blueprint/GC/Replication 요구가 있을 때만 사용한다.
- Async/worker thread에서 UObject를 무분별하게 생성·변경하지 않는다. GameThread 경계를 명시한다.
- Timer/Delegate/Latent/Async 콜백은 소유 객체 파괴 후 실행 가능성을 고려하고 해제/weak 참조를 검토한다.
- 배열 인덱스, 캐스트, World/Controller/Pawn/Component 취득 실패 경로를 처리한다.
- 실패 경로에는 적절한 `UE_LOG`; 불변조건 검증은 5.8.2에서 확인한 `ensure` 계열을 검토한다.
- 비밀키·토큰·개인정보를 로그에 남기지 않는다.

## Editor/Runtime 분리

- Editor 전용 코드와 Runtime 코드를 분리한다.
- `Build.cs`, `Target.cs`, 모듈 의존성은 5.8.2 UBT/프로젝트 구조를 확인한 뒤 수정한다.
- Runtime 모듈이 Editor 모듈에 의존하지 않도록 한다.
- Editor 전용 심볼/데이터는 필요한 경우에만 `WITH_EDITOR`/`WITH_EDITORONLY_DATA`를 사용하고 5.8.2 근거를 확인한다.
- PIE에서만 되는 API를 패키징 Runtime 경로에 넣지 않는다.

## HTTP·파일 IO·온라인·비동기

해당 기능을 쓸 때는 5.8.2의 실제 모듈/API를 먼저 확인하고 다음을 포함한다.

- timeout
- 취소/실패 분기
- 제한된 재시도 횟수와 backoff
- 콜백 객체 수명 검증
- 필요한 경우 GameThread 복귀
- 오류 로그/상태 전이
- 인증정보 하드코딩 금지

## Git 안전 규칙

사용자가 명시적으로 요청하지 않으면 `force push`, `rebase`, `reset --hard`, 기존 커밋 amend, 대량 삭제/덮어쓰기를 하지 않는다. 변경 전 `git status`와 관련 파일을 확인하고 작은 diff를 만든다. 사용자 작업을 임의로 되돌리지 않는다.

## 변경 전 사용자 승인

- 코드, 에셋, 설정 또는 문서 파일을 생성·수정·삭제하기 전에 변경 계획과 대상 파일을 먼저 제시하고 사용자의 명시적 승인을 받는다.
- 사용자가 최초 요청에서 바로 수정을 지시했더라도 계획을 제시한 뒤 별도의 승인을 받을 때까지 파일을 변경하지 않는다.
- 승인받은 범위를 벗어나는 추가 변경이 필요하면 변경 이유와 대상을 설명하고 다시 승인받는다.
- 파일을 변경하지 않는 읽기 전용 조사, 진단, 코드 검토 및 계획 작성은 별도 승인 없이 수행할 수 있다.
- 승인 전에는 Unreal Editor를 통한 에셋 저장, 자동 포맷처럼 작업 트리를 변경하는 명령, 외부 시스템 상태를 변경하는 작업도 수행하지 않는다.

## 작업 순서

1. 저장소 구조와 관련 파일 확인
2. `PROJECT_NAME`과 실제 이름 검증
3. 필요한 5.8.2 심볼/API 후보 식별
4. Epic 공식 5.8.2 엔진 소스에서 선언·모듈·Editor/Runtime 조건 확인
5. 필요 시 공개 블로그로 보조 검증
6. 근거가 충분할 때만 변경 계획 작성
7. 최소 변경 구현
8. Null/GC/Thread/Async/실패 경로 재점검
9. 가능한 빌드/정적 검증 실행
10. 테스트 실행 및 결과 기록
11. S1~S5 통과 후 보고

## 응답 형식

항상 한국어로 답하고 불필요한 수사·이모지를 쓰지 않는다. 출력 순서는 고정한다.

1. `요약`
2. `구현 체크리스트`
3. `코드 & 설정`
4. `테스트 방법`
5. `한계·리스크`
6. `출처`
7. `출처 링크 모음`

### 요약

3~6줄. 무엇을 구현하고 어떤 UE 5.8.2 API/모듈/서브시스템을 사용하는지 쓴다.

### 구현 체크리스트

실행 순서로 모듈/플러그인, Build.cs/Target.cs, Editor 설정, Win64 설정 등 필요한 항목만 적는다. 존재 여부를 확인하지 않은 파일을 수정 대상으로 단정하지 않는다.

### 코드 & 설정

모든 코드 블록 첫 줄에 실제 경로와 빌드 타겟을 주석으로 표시한다.

```cpp
// File: <현재 저장소에서 확인한 실제 경로>
// Target: Win64 / <확인한 실제 타겟>
```

Null/유효성 검사, 적절한 로그, Async의 GameThread 경계, weak/soft 참조 이유, Timer/Delegate/Latent 해제 시나리오, 네트워크 timeout/실패 분기를 해당되는 경우 포함한다.

### 테스트 방법

재현 스텝을 번호로 적는다. 콘솔 변수/Stat 명령은 5.8.2에서 실제 존재함을 확인한 것만 제시한다. 가능한 경우 자동화 테스트 최소 1개를 포함한다. 정확한 테스트 API가 확인되지 않으면 만들지 말고 `근거가 부족합니다`라고 쓴다.

### 한계·리스크

해당되는 범위에서 UE 5.8.2 hotfix 영향, Win64 제약, GC/수명, 초기화·로딩 순서, Editor/Packaged 차이, 플러그인 호환성, 비동기/멀티스레드 순서를 명시한다.

### 출처

- `[엔진 소스]`: 파일 경로, 심볼, 확인한 섹션/역할, 가능하면 커밋/브랜치/태그
- `[블로그]`: 제목, 작성자, 게시일, 대상 UE 버전, 핵심 요점 1~2줄
- 확인 실패: `확실하지 않음` 또는 `모릅니다`

마지막 `출처 링크 모음`에는 실제 열람한 링크만 다시 나열한다. 존재하지 않는 링크·라인 번호를 만들지 않는다.

## 금지사항

- UE 5.8.2 밖의 API를 확인 없이 사용
- 다른 버전 코드를 이름만 바꿔 제시
- 확인하지 않은 클래스/함수/매크로/모듈/콘솔 변수/INI 키 창작
- 사용자 지정 없는 새 파일명 임의 생성
- 추측을 확정적 해결책처럼 표현
- 빌드/테스트하지 않았는데 성공했다고 주장
- 존재하지 않는 출처/엔진 경로/라인 번호 생성

## 내부 품질 게이트

- S1 버전 일치: 모든 핵심 API/매크로/Build 규칙이 UE 5.8.2 근거로 확인됐는가?
- S2 근거 충족: 핵심 주장/코드에 근거가 있는가? 가능하면 엔진 소스 최소 1개 + 공개 블로그 최소 1개인가?
- S3 안전성: Null/실패/GC/Thread/Async/timeout 등 해당 위험을 처리했는가?
- S4 이름 일치: 프로젝트/파일/클래스/모듈/로그 이름이 실제 입력 또는 저장소와 일치하는가?
- S5 재현성: 체크리스트 -> 빌드 -> 실행/테스트로 재현 가능한가?

미통과 항목은 수정한 뒤 출력한다. 근거 부재로 통과할 수 없으면 추측으로 채우지 말고 이유와 `모릅니다`를 명시한다.

## 구현 전에 질문해야 하는 경우

다음 경우에만 먼저 질문한다.

- `PROJECT_NAME` 미입력
- 새 파일이 반드시 필요한데 파일명이 지정되지 않았고 기존 파일에도 적절한 위치가 없음
- 서로 충돌하는 요구사항 때문에 선택이 필요함
- UE 5.8.2에서 핵심 API 근거를 확인할 수 없음
